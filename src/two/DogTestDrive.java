package two;

class DogTestDrive {
    public static void main(String[] args) {
        // Создаем объект класса Dog
        Dog d = new Dog();
        // Устанавливаем значение поля size
        d.size = 40;
        // Вызываем метод bark()
        d.bark();
    }
}
